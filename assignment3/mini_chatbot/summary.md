# 과제 요약 보고서

## 1. 구현한 API 요약

* **엔드포인트:** `POST /chat`
* **요청 (Request) JSON 예시:**
    ```json
    { 
      "message": "LLMOps가 뭐야?", 
      "prompt_version": "v1" 
    }
    ```
* **응답 (Response) JSON 예시:**
    ```json
    { 
      "reply": "LLMOps는...", 
      "model": "gpt-4o-mini" 
    }
    ```

## 2. 실험 설정

* **프롬프트 버전 (System Prompt):**
    * `v1`: "당신은 친절하고 간결하게 답변하는 AI 챗봇입니다."
    * `v2`: "당신은 AI 전문가입니다. 질문에 대해 상세하고, 구조화된 답변을 제공해야 합니다."
* **사용 모델:** `gpt-4o-mini`
* **총 호출 횟수:** 6회

## 3. 간단 결과

> `analyze_logs.py` 스크립트 실행 결과를 여기에 붙여넣으세요.

**[1. 프롬프트 버전(v1/v2)별 평균 지연시간 (ms)]**

| prompt_version   |   latency_ms |
|:-----------------|-------------:|
| v1               |      4023.07 |
| v2               |     15060.42 |

**[2. 프롬프트 버전(v1/v2)별 평균 토큰 수]**

| prompt_version   |   total_tokens |
|:-----------------|---------------:|
| v1               |         165.00 |
| v2               |         695.67 |

**[3. 사용 모델별 평균 지연시간 (ms)]**
| model                  |   latency_ms |
|:-----------------------|-------------:|
| gpt-4o-mini-2024-07-18 |      9541.75 |

## 4. 느낀 점 (인사이트)

v2 프롬프트는 v1 대비 평균 토큰 4.2배, 응답 시간 3.7배가 더 소모되어, 상세한 답변 품질을 얻는 대신 비용과 대기 시간이라는 명확한 트레이드오프가 발생했다.
실제 운영 시 v2를 그대로 사용하면 15초의 응답 속도는 사용자 이탈을 유발하고 4.2배의 API 비용은 큰 부담이 될 것이다.
따라서 v1(간결/빠름)을 기본으로 제공하고, 상세 답변(v2)은 사용자가 선택적으로 요청하게 하는 방식이 가장 현실적이고 합리적인 운영 전략이라고 판단된다.